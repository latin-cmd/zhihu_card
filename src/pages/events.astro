---
import { currentCardSpace } from "../lib/card-space-store";

const space = await currentCardSpace(Astro.request);
if (!space) return Astro.redirect("/credentials/zhihu?next=/events", 302);
return Astro.redirect(`/spaces/${space.id}/events`, 302);
---
