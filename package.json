{
  "name": "communist-activity-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "mcp": "node mcp/server.mjs",
    "directus:bootstrap": "node scripts/bootstrap-directus.mjs",
    "event:delete": "node scripts/delete-event.mjs",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "cf:dry-run": "astro build && wrangler deploy --dry-run",
    "cf:deploy": "astro build && wrangler deploy",
    "cf:types": "wrangler types"
  },
  "dependencies": {
    "@lucide/astro": "^1.26.0",
    "@modelcontextprotocol/sdk": "^1.17.0",
    "astro": "^7.1.3",
    "zod": "^4.4.3"
  },
  "overrides": {
    "@hono/node-server": "^2.0.5"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^14.1.4",
    "@cloudflare/workers-types": "^5.20260724.1",
    "wrangler": "^4.114.0"
  }
}
